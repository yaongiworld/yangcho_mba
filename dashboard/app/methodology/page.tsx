/**
 * Methodology Showcase — page 4.
 *
 * Yangcho's 3 hand-written hero case studies in first person, plus the scoring
 * formula, data sources, and the 1-paragraph framework explainer. Surfaces
 * "Last successful pipeline run: [timestamp]" as transparency signal.
 *
 * Hero case studies render long-form (essay-quality typography). This page is
 * what survives any technical interview challenge — it shows Yangcho's reasoning,
 * not the AI's.
 *
 * Implementation lands in W5 (page) + W1 (Yangcho writes the cases).
 */

export default function MethodologyPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-12">
      <h1 className="text-3xl font-semibold">Methodology</h1>
      <p className="mt-3 text-neutral-500">
        Three hero case studies + scoring formula + data sources. Placeholder — W5.
      </p>
    </main>
  );
}
